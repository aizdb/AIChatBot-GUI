# AuroraChat
Here’s a clean, professional, fully standalone chatbot with modular NLU, short-term memory, retrieval-based answers, and pluggable skills — no external APIs, just Python. It runs via CLI or a lightweight web server (Flask).
